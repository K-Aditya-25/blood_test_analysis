# This directory holds the generated report data.
# `report.json` is produced by the Python pipeline (`python -m src.export_json`
# from the repo root) and is git-ignored because it contains medical data.
